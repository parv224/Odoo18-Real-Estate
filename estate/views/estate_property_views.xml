<odoo>

    <!-- SEARCH VIEW -->
    <record id="estate_property_view_search" model="ir.ui.view">
        <field name="name">estate.property.search.view</field>
        <field name="model">estate.property</field>
        <field name="arch" type="xml">
            <search>
                <field name="name" filter_domain="[('name','ilike',self)]"/>
                <field name="postcode"/>
                <field name="expected_price"/>
                <field name="bedrooms"/>
                <field name="facades"/>
                <field name="state" invisible="1"/>
                <separator>
                    <group string="Filter">
                        <filter name="available" string="Available" domain="[('state','in',('new','offer_received'))]"/>
                        <filter name="filter_new_properties" string="New Properties" domain="[('state','=','new')]"/>
                        <filter name="filter_offer_received" string="Offer Received" domain="[('state','=','offer_received')]"/>
                    </group>
                </separator>
                <group string="Group By">
                    <filter name="group_by_postcode" string="Postcode" context="{'group_by': 'postcode'}"/>
                </group>
            </search>
        </field>
    </record>

    <!-- LIST VIEW -->
    <record id="estate_property_view_list" model="ir.ui.view">
        <field name="name">estate.property.list.view</field>
        <field name="model">estate.property</field>
        <field name="arch" type="xml">
            <list string="Properties"
                  decoration-success="state in ('offer_received', 'offer_accepted')"
                  decoration-danger="state in ('cancelled')"
                  decoration-bf="state == 'offer_accepted'"
                  decoration-muted="state == 'sold'">
                <field name="name" string="Title"/>
                <field name="property_type_id"/>
                <field name="postcode"/>
                <field name="tags_ids" widget="many2many_tags" options="{'color_field': 'color'}"/>
                <field name="bedrooms"/>
                <field name="living_area"/>
                <field name="expected_price"/>
                <field name="selling_price"/>
                <field name="date_availability" options="{'no_create': True}"/>
                <field name="state"/>
            </list>
        </field>
    </record>

    <!-- FORM VIEW -->
    <record id="estate_property_view_form" model="ir.ui.view">
        <field name="name">estate.property.form.view</field>
        <field name="model">estate.property</field>
        <field name="arch" type="xml">
            <form string="Real Estate">
                <header>
                    <button name="action_sold" invisible="state in ('sold','cancelled')" type="object" string="Sell" class="btn btn-primary"/>
                    <button name="action_cancel" invisible="state in ('sold','cancelled')" type="object" string="Cancel"/>
                    <field name="state" widget="statusbar"/>
                </header>

                <sheet>
                    <widget name="web_ribbon" title="Archived" bg_color="bg-danger" invisible="active"/>
                    <group>
                            <group>
                                <field name="name" class="oe_title"/>
                                <field name="property_type_id"/>
                                <field name="total_area"/>
                                <field name="postcode"/>
                                <field name="date_availability"/>
                            </group>

                            <group>
                                <field name="expected_price"/>
                                <field name="best_offer" readonly="1"/>
                                <field name="selling_price" readonly="1"/>
                                <field name="tags_ids" widget="many2many_tags" options="{'color_field': 'color'}"/>

                            </group>
                        </group>

                    <!-- NOTEBOOK (TABS) -->
                    <notebook>
                        <!-- TAB 1 : DESCRIPTION -->
                        <page string="Description">
                            <group>
                                <field name="description"/>
                                <group>
                                    <field name="bedrooms"/>
                                    <field name="living_area"/>
                                    <field name="facades"/>
                                    <field name="garage"/>
                                    <field name="garden"/>
                                    <field name="garden_area"/>
                                    <field name="garden_orientation" widget="radio"/>
                                    <field name="total_area" readonly="1"/>
                                </group>
                            </group>
                        </page>

                        <!-- TAB 2 : OFFERS -->
                        <page string="Offers">
                            <field name="offer_ids"
            mode="list,form"
            context="{'form_view_ref': 'estate.estate_property_offer_view_form'}">
                                <list string="Offers" decoration-success="status == 'accepted'" decoration-danger="status == 'refused'">
                                    <field name="partner_id"/>
                                    <field name="price"/>
                                    <field name="validity"/>
                                    <field name="date_deadline" column-invisible="parent.status == 'sold'"/>
                                    <field name="status" options="False"/>
                                    <button icon="fa-check" invisible="status in ('accepted','refused')" name="action_accept" type="object" title="accept" />
                                    <button icon="fa-times" invisible="status in ('accepted','refused')" name="action_refuse" type="object" title="refuse" />
                                </list>
                            </field>
                        </page>

                        <!-- TAB 3 : OTHER INFO -->
                        <page string="Other Info">
                            <group>
                                <field name="property_type_id"/>
                                <field name="salesperson_ids"/>
                                <field name="buyer_ids" readonly="1"/>
                            </group>
                        </page>

                    </notebook>

                </sheet>
            </form>
        </field>
    </record>


    <!-- ACTION -->
    <record id="action_estate_property" model="ir.actions.act_window">
        <field name="name">Real Estate</field>
        <field name="res_model">estate.property</field>
        <field name="view_mode">list,form</field>
        <field name="context">{'search_default_available': True}</field>
    </record>

    <record id="action_estate_property_tags" model="ir.actions.act_window">
        <field name="name">Tags</field>
        <field name="res_model">estate.property.tags</field>
        <field name="view_mode">list,form</field>
    </record>

    <record id="action_estate_property_type" model="ir.actions.act_window">
        <field name="name">Property Types</field>
        <field name="res_model">estate.property.type</field>
        <field name="view_mode">list,form</field>
    </record>

</odoo>
